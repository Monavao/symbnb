<!DOCTYPE html>
<html lang="fr" class="h-100">
<head>
    <meta charset="UTF-8">
    <title>SymBnB - Connection à l'administration</title>
    <meta name="viewport" content="width=device-width, user-scalable=no">
    <link rel="stylesheet" href="{{ asset('css/bootstrap-4-4-1.min.css') }}">
    <link rel="stylesheet" href="{{ asset('css/bootswatch.min.css') }}">
{#    <link rel="stylesheet" href="{{ asset('css/main.css') }}">#}
</head>
<body class="d-flex flex-column h-100">
<div class="container mt-5">
    <div class="row">
        <div class="col-md-3">&nbsp;</div>
        <div class="col-md-6">
            <div class="card bg-light">
                <div class="card-header">
                    <h1 class="card-title h4"><i class="fas fa-lock"></i> Connexion à l'administration</h1>
                    {% if hasError %}
                        <div class="alert alert-danger text-center">
                            Adresse email et/ou mot de passe incorrect
                        </div>
                    {% endif %}
                </div>
                <div class="card-body">
                    <form method="post">
                        <div class="form-group">
                            <label for="_username">Email</label>
                            <input type="text" class="form-control" name="_username" value="{{ username }}" required>
                        </div>
                        <div class="form-group">
                            <label for="_password">Mot de passe</label>
                            <input type="password" class="form-control" name="_password" required>
                        </div>
                        <div class="form-group mt-4">
                            <div class="text-center">
                                <button type="submit" class="btn btn-primary w-50"><i class="fas fa-lock-open"></i> Connexion</button>
                                <input type="hidden" name="_csrf_token" value="{{ csrf_token('authenticate') }}">
                                <a href="{{ path('home') }}" class="btn btn-link"><i class="fas fa-arrow-circle-left"></i> Retour au site</a>
                            </div>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>


<script src="{{ asset('js/jquery-3-4-1.min.js') }}"></script>
<script src="{{ asset('js/popper-1-16-0.min.js') }}"></script>
<script src="{{ asset('js/bootstrap-4-4-1.min.js') }}"></script>
<script src="https://kit.fontawesome.com/2a172cacbc.js" crossorigin="anonymous"></script>
</body>
</html>