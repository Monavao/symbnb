{% extends 'base.html.twig' %}

{% block title %}Administration des annonces{% endblock %}

{% block body %}
    <div class="container">
        <h1 class="my-5">Gestion des annonces</h1>
        <table class="table table-striped text-center align-content-center">
            <thead>
            <tr>
                <th>Id</th>
                <th>Titre</th>
                <th>Auteur</th>
                <th>Réservations</th>
                <th>Note</th>
                <th>Actions</th>
            </tr>
            </thead>
            <tbody>
            {% for ad in ads %}
                <tr>
                    <td>{{ ad.id }}</td>
                    <td>{{ ad.title }}</td>
                    <td>{{ ad.author.fullName }}</td>
                    <td><span class="badge badge-primary rounded">{{ ad.bookings | length }}</span></td>
                    <td><span class="badge badge-primary rounded">{{ ad.avgRatings | number_format(1, ',', ' ') }}</span></td>
                    <td>
                        <a href="#" class="btn btn-primary"><i class="fas fa-edit"></i></a>
                        <a href="#" class="btn btn-danger"><i class="fas fa-trash"></i></a>
                    </td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
{% endblock %}
