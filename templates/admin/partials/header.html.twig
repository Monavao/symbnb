<nav class="navbar navbar-expand-lg navbar-dark bg-primary">
    <a class="navbar-brand" href="{{ path('home') }}">SymBnB</a>
    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarColor03" aria-controls="navbarColor03" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="collapse navbar-collapse" id="navbarColor03">
        <ul class="navbar-nav mr-auto">
            <li class="nav-item">
                <a class="nav-link" href="#"><i class="fas fa-chart-line"></i> Dashboard</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_ads') }}"><i class="fas fa-hotel"></i> Annonces</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_bookings') }}"><i class="fas fa-atlas"></i> Réservations</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="{{ path('admin_comments') }}"><i class="fas fa-comments"></i> Commentaires</a>
            </li>
            <li class="nav-item">
                <a class="nav-link" href="#"><i class="fas fa-users"></i> Utilisateurs</a>
            </li>
        </ul>
        <ul class="navbar-nav ml-auto">
            <li class="nav-item">
                <a href="{{ path('home') }}" class="nav-link"><i class="fas fa-eye"></i> Aller vers le site</a>
            </li>
            {% if app.user %}
                <li class="nav-item dropdown">
                    <a href="#" class="nav-link dropdown-toggle" data-toggle="dropdown" id="accountDropdownLink">
                        {% if app.user.picture %}
                            <img class="avatar avatar-mini" src="{{ vich_uploader_asset(app.user, 'pictureFile') }}" alt="avatar-{{ app.user.fullName }}"> {{ app.user.fullName }}
                        {% else %}
                            <img class="avatar avatar-mini" src="{{ asset('/images/empty-avatar.png') }}" alt="avatar-{{ app.user.fullName }}"> {{ app.user.fullName }}
                        {% endif %}
                    </a>
                    <div class="dropdown-menu dropdown-menu-right" aria-labelledby="accountDropdownLink">
                        <a href="{{ path('admin_account_logout') }}" class="dropdown-item">Déconnexion</a>
                    </div>
                </li>
            {% endif %}
        </ul>
    </div>
</nav>

{% for label, messages in app.flashes %}
    <div class="container mt-2">
        <div class="alert alert-{{ label }}">
            {% for message in messages %}
                <p>{{ message | raw }}</p>
            {% endfor %}
        </div>
    </div>
{% endfor %}